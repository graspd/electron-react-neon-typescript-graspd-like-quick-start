import React, { FC } from "react";

interface AppProps {}

const App: FC<AppProps> = (): JSX.Element => {
  return (
    <div>
      <h1>Borderplate</h1>
      <p>Some borderplate code</p>
    </div>
  );
};

export default App;
