import React, { FC } from "react";

interface AppProps {}

const App: FC<AppProps> = (): JSX.Element => {
  return (
    <div>
      <h1>Boilerplate</h1>
      <p>Some boilerplate code</p>
    </div>
  );
};

export default App;
